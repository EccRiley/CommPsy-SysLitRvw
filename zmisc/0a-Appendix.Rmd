---
title: "MAP - Appendices"
author: "Rachel M. Smith"
date: "`r format(Sys.Date(), '%d %B %Y')`"
fignos-plus-name: "Figure "
tablenos-plus-name: "Table "
---
\Frule

\newpage

<!-- doublespacing -->

# Appendix A: Referenced Tables \& Figures

```{r tbldbsrch}
## BELOW IS FOR .DOCX OUTPUT ##
# dbsrch$srch <- paste0("_", dbsrch$srch, "_")
# dbsrch$n <- paste0("_", dbsrch$n, "_")
# dbsrch$nres <- paste0("_", dbsrch$nres, "_")
# names(dbsrch) <- c("", "Database Search", "$Range_{N_{Results}}$")

# kable(dbsrch, caption = "Descriptions of database searches conducted with corresponding ranges of the number of results returned {#tbl:dbsrch}", align = c("r", "l", "c"))

pander(dbsrch, caption = "Descriptions of database searches conducted with corresponding ranges of the number of results returned^a^ {#tbl:dbsrch}", justify = c("right", "left", "left"))
```

\tufteskip

\footnotesize{$^a$ For each database search, multiple search terms were included for the subject/keywords parameters to represent intimate partner violence}

<!-- TODO: (`{{see Appendix ??? for the specific terms used in the command-line codes entered for each database search}}`) -->

\newpage

```{r tbljcp}

jlvls <- levels(MAP$journal)
jncp <- j.cppr[!levels(factor(j.cppr)) %in% jlvls]

levels(MAP$journal) <- c(jlvls, jncp)

jcpv <- Rtdf(MAP$journal, names = c("j", "frq"))

jcpv$cpv <- ifelse(jcpv$j %in% j.cppr, "Community Psychology", "Violence")
# jcpv <- jcpv[, c("j", "cpv", "frq"), drop = FALSE]

jv <- jcpv[jcpv$cpv == "Violence", c("j", "frq")]
# jv$frq <- ifelse(jv$frq == 0, NA, jv$frq) %>% na.omit()

jcp <- jcpv[jcpv$cpv != "Violence", c("j", "frq")]
# jcp$frq <- ifelse(jcp$frq == 0, NA, jcp$frq) %>% na.omit()


kable(jcp, col.names = c("Publication Title", "$N_{articles}$"), caption = "_Community-Psychology_-Specific Journal Titles Included in Literature Database Searches with the Corresponding Number of Formally Reviewed Articles per Journal {#tbl:jcp}")

# kable(jcp, col.names = c("Publication Title", "$N_{articles}$"), caption = "_Community-Psychology_-Specific Journal Titles Included in Literature Database Searches with the Corresponding Number of Articles per Journal Returned from the First Database Search (i.e., \"IPV - General\") {#tbl:jcp}")
```

\newpage

```{r tbljv}
kable(jv, col.names = c("Publication Title", "$N_{articles}$"), caption = "_Violence_-Specific Journal Titles Included in Literature Database Searches with the Corresponding Number of Formally Reviewed Articles per Journal Included {#tbl:jv}")

# kable(jv, col.names = c("Publication Title", "$N_{articles}$"), caption = "_Violence_-Specific Journal Titles Included in Literature Database Searches with the Corresponding Number of Articles per Journal Returned from the First Database Search (i.e., \"IPV - General\") {#tbl:jv}")
```

\newpage

```{r tbljsft}
MAP$journal <- droplevels(MAP$journal)
# Rtdf(MAP$journal, names = c("Publication Title", "$N_{Articles}$")) %>%
#     kable(caption = "Publication Titles of Reviewed Articles {#tbl:mapJrnl}")

MAPp <- MAP[, c("journal", "scat")] %>% dplyr::rename("Publication Title" = journal, "Research Category" = scat)

MAPp[, 2] <- factor(MAPp[, 2],
                    labels = c("IPV Interventions Research",
                               "SMW-Inclusive IPV Research"))

jsft1 <- ftable(MAPp) %>% as.matrix()
jsft2 <- ifelse(jsft1 == 0, NA, jsft1)
jsftt <- apply(jsft2, 1, sum, na.rm = TRUE)
jsftt <- paste0("**", jsftt, "**")
jsft <- cbind(jsft2, jsftt)

kable(jsft, col.names = c(colnames(jsft)[1:2], "$N_{Articles_{Total}}$"), caption = "Number of Formally Reviewed Articles per Journal in Each Research Domain Covered in this Review {#tbl:jsft}", align = c("c", "c", "c"))

# colnames(jsft) <- c(colnames(jsft)[1:2], "$N_{Articles_{Total}}$")

# pander(jsft, caption = "Number of Formally Reviewed Articles per Journal in Each Research Domain Covered in this Review {#tbl:jsft}", justify = c("left", "centre", "centre", "centre"))
```

\newpage

```{r cdbk, opts.template='invisible'}
source("cdbk.R")
```

```{r tblcdbk}
cdbk.ft$clab <- gsub("\\n", "", cdbk.ft$clab)
kable(cdbk.ft, col.names = c("Information Category", "Codes"), caption = "Codebook Constructed from the Discrete Summative Data Compiled Across the Formally Reviewed Literature {#tbl:cdbk}", align = c("r", "l"))
```

\newpage

![Social-Ecological Model](graphics/inputs/sem.pdf){#fig:sem}

\newpage

![Timeline of publication years of the reviewed literature](graphics/inputs/yrhist.pdf){#fig:yrhist}

\newpage

![Systematic Literature Search and Inclusion Flow-Chart](graphics/inputs/flowchart.png){#fig:flowchart}

\newpage

![Network graph of the levels of analysis involved in the reviewed literature](graphics/inputs/keysnet.pdf){#fig:keysnet}

\newpage

![`r alnetg_cap`](graphics/inputs/arc_analyses.png){#fig:arc_analyses}

<!-- `pander(as.list(alabs))` -->

# Appendix B. Literature Description & Data Extraction Template

\Frule

<!-- TODO: INCLUDE APPENDIX B CONTENT -->

# Appendix C. Qualitative Comparative Analysis

<!-- TODO: INCLUDE APPENDIX C CONTENT -->

\part{References}

\refs
